const Header = () => {
    return (
        <header className="w-full bg-gray-800 text-white text-center py-4 text-2xl font-semibold shadow">
            Header
        </header>
    );
};

export default Header;
